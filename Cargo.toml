[package]
name = "aws-billing-info"
version = "0.1.0"
edition = "2021"
description = "Minimal TUI for AWS billing"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
serde_json = "1.0"
chrono = "0.4"
thiserror = "1.0"
toml = "0.8"
dirs = "5.0"
